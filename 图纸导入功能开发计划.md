# 图纸导入功能开发计划

更新时间：2026-06-08

关联设计文档：

- `图纸导入功能实现设计.md`

## 1. 开发原则

图纸导入功能按“先闭环、再识别、再智能化”的优先级推进。

核心原则：

- 最终产物必须是现有 `PatternResult`，保证结果页、编辑器、下载、库存和专注模式可复用。
- 先支持结构化和自有格式，建立稳定导入闭环。
- 图片识别必须允许用户手动修正，不能把自动识别失败变成导入失败。
- 色号系统识别必须可解释，低置信度和冲突色号必须进入确认流程。
- `PatternResult` 保持干净，导入诊断信息放在独立 import meta / analysis 中。

## 2. 优先级总览

| 优先级 | 阶段 | 目标 | 用户价值 | 技术风险 |
|---|---|---|---|---|
| P0 | 基础闭环 | 支持 JSON / CSV 导入并保存为项目 | 有真实可用入口 | 低 |
| P1 | 自有图纸回导 | Dodoudou 导出图纸可无损再导入 | 分享和备份可回收 | 低 |
| P2 | 清晰图片导入 | 识别截图 / PNG / JPG 中的网格和色块 | 覆盖最常见导入场景 | 中 |
| P3 | 色号系统识别 | 图例 OCR、品牌推断、冲突确认 | 正确还原物料清单 | 中高 |
| P4 | PDF 导入 | PDF 页面渲染、文本层辅助、多页选择 | 覆盖官方图纸和生成器导出 | 中高 |
| P5 | 拍照和高级修复 | 透视矫正、阴影校正、局部修复 | 覆盖纸质图纸 | 高 |

建议严格按 P0 -> P5 推进。P0 和 P1 是产品闭环基础，P2 之后才进入图像识别复杂区。

## 3. P0：基础导入闭环

目标：

- 打通“导入文件 -> 解析 -> 生成 `PatternResult` -> 保存项目 -> 进入结果页”的完整流程。
- 支持稳定、可测试的结构化格式。

范围：

- Dodoudou JSON 导入。
- CSV / TSV 矩阵导入。
- 基础色号解析。
- `PatternResult` 校验和统计重建。
- 新增导入页面和首页入口跳转。

推荐文件：

```text
src/lib/pattern-import/
  types.ts
  validate.ts
  structured.ts
  reconstruct.ts
  diagnostics.ts

src/pages/workshop/import/
  WorkshopImportPage.tsx
  WorkshopImportPage.module.css
```

需要改动：

- `src/app/App.tsx`
  - 新增 `/workshop/import` 路由。
- `src/pages/workshop/WorkshopHomePage.tsx`
  - “导入图纸”入口从开发中 toast 改成跳转导入页。
- `src/features/workshop/model/projectStore.ts`
  - `WorkshopProjectSourceType` 增加 `'import'`。
  - 可选增加 `importMeta`。

任务拆解：

1. 定义导入类型。
2. 实现 `validatePatternResult`。
3. 实现 `rebuildPatternPaletteFromCells`。
4. 实现 Dodoudou JSON parser。
5. 实现 CSV / TSV parser。
6. 实现导入页面文件选择。
7. 解析成功后创建项目并保存。
8. 跳转 `/workshop/result/:projectId`。
9. 导入失败时展示可读错误。

验收标准：

- JSON 导入后能进入结果页。
- CSV 导入后能进入结果页。
- 导入图纸能进入编辑器。
- 导入图纸能下载 PNG。
- 导入图纸能打开物料清单。
- 导入图纸能进入专注模式。
- `npm run check` 通过。

预计工作量：

- 1.5 - 2.5 个开发日。

## 4. P1：Dodoudou 自有图纸回导

目标：

- Dodoudou 导出的图纸可以被 Dodoudou 无损导入。
- 形成“导出即备份”的能力。

范围：

- 导出时附带一份可回导的结构化数据。
- 导入时优先读取自有元数据。
- 无损恢复 `PatternResult`。

实现建议：

优先级从高到低：

1. 导出 PNG 时同时允许下载 `.dodoudou.json`。
2. 导出 PNG 中写入压缩后的 metadata。
3. 后续再考虑二维码或短码回导。

第一版建议先做旁路 JSON，因为浏览器写 PNG metadata 兼容性和实现成本更高。

需要改动：

- `src/lib/pattern/download.ts`
  - 新增导出结构化 JSON 的能力。
- `src/pages/workshop/DownloadSettingsModal.tsx`
  - 增加“同时导出可回导数据”选项。
- `src/lib/pattern-import/structured.ts`
  - 识别 Dodoudou JSON schema version。

任务拆解：

1. 定义 Dodoudou pattern export schema。
2. 给 schema 增加 `version`、`app`、`createdAt`、`patternResult`、`config`。
3. 下载弹窗增加导出 JSON 选项。
4. 导入页识别 JSON 并校验版本。
5. 导入后保持原图纸尺寸、色号和 palette 统计一致。

验收标准：

- 从结果页导出的 JSON 可再次导入。
- 回导后的 `PatternResult` 与原始图纸一致。
- 回导后编辑、下载、库存和专注模式可用。
- 老导出流程不受影响。

预计工作量：

- 1 - 1.5 个开发日。

## 5. P2：清晰图片导入

目标：

- 支持用户上传清晰截图、PNG、JPG 图纸。
- 自动识别矩形网格、行列数和每格颜色。
- 无法稳定识别时允许用户手动修正。

范围：

- 图片解码。
- 网格区域自动检测。
- 手动框选网格区域。
- 行列数推断和手动输入。
- 单元格颜色采样。
- 图例 swatch 简单识别。

推荐文件：

```text
src/lib/pattern-import/
  image-decode.ts
  layout-detect.ts
  grid-detect.ts
  cell-read.ts
  color-resolve.ts
  confidence.ts
```

任务拆解：

1. 图片文件转 Canvas / ImageData。
2. 限制超大图片尺寸，保留原始宽高。
3. 检测横线、竖线和规则网格候选。
4. 输出多个 `PatternImportGridCandidate`。
5. 导入页展示候选网格框。
6. 支持用户拖拽四角 / 调整区域。
7. 支持用户输入 `columns`、`rows`。
8. 每格采样中心安全区域。
9. 对采样颜色做聚类，生成临时 palette。
10. 转换为 `PatternResult`。
11. 低置信度格子高亮。

算法约束：

- 不要求第一版识别格内文字。
- 不要求第一版识别复杂符号。
- 不要求第一版支持透视严重的照片。
- 必须支持非正方形图纸。

验收标准：

- 清晰 48x48、100x100 图纸可自动识别行列数。
- 用户能手动修正行列数。
- 纯色块图纸能还原大部分颜色区域。
- 低置信度格子能在预览中定位。
- 导入失败不会生成坏项目。

预计工作量：

- 3 - 5 个开发日。

## 6. P3：图例、OCR 和色号系统识别

目标：

- 从图例中识别色号、颜色名和数量。
- 推断图纸使用的品牌色号系统。
- 将外部图纸颜色解析到内部品牌色卡。

范围：

- 图例区域检测。
- 图例条目切分。
- OCR 接入。
- 色号规范化。
- 品牌推断。
- 冲突确认页面。

推荐文件：

```text
src/lib/pattern-import/
  legend-detect.ts
  color-system-detect.ts
  color-resolve.ts
```

任务拆解：

1. 识别图例区域和色块。
2. 将图例拆成 swatch、rawCode、rawName、rawCount。
3. 接入 OCR 引擎。
4. 实现 `normalizeImportedColorCode`。
5. 基于 `getBrandPalette` 和 `getColorByBrandCode` 做精确色号匹配。
6. 基于 swatch Lab 色差做 nearest 匹配。
7. 实现品牌评分。
8. 实现重复码和冲突色号处理。
9. 导入确认页展示未识别色、冲突色和数量差异。
10. 用户确认后生成最终 `PatternResult`。

色号解析优先级：

1. 用户指定品牌 + 色号精确命中。
2. 自动识别品牌 + 色号精确命中。
3. 外部别名表命中。
4. 图例 swatch 最近色。
5. 跨品牌同 hex。
6. 近似色推荐，用户确认。
7. 外部色导入。

验收标准：

- 清晰图例中的色号能被识别并展示。
- 自动推荐品牌时显示置信度。
- 重复码不会静默误选。
- 图例数量和重建数量不一致时能提示。
- 用户能手动修正 OCR 错误。

预计工作量：

- 4 - 7 个开发日。

## 7. P4：PDF 导入

目标：

- 支持官方图纸 PDF 和生成器导出的 PDF。
- 利用 PDF 文本层提升图例和数量识别准确率。
- 支持多页选择。

范围：

- PDF 文件解码。
- 每页渲染成图片。
- 文本层提取。
- 页面选择。
- 单页图纸导入。
- 多页图纸的手动拼接预留。

任务拆解：

1. 选择 PDF 解析方案。
2. PDF 页渲染为 ImageData。
3. 提取每页 text items。
4. 自动识别可能的图纸页和图例页。
5. 导入页增加页面选择。
6. 单页 PDF 走 P2/P3 图片识别流程。
7. 文本层辅助图例识别。
8. 多页大图纸先支持用户选择其中一页导入。
9. 后续扩展多页拼接。

验收标准：

- 单页 PDF 可以导入。
- PDF 文本层中的色号和数量可以参与图例识别。
- 多页 PDF 可以选择页面。
- PDF 解析失败时可提示用户转图片导入。

预计工作量：

- 3 - 5 个开发日。

## 8. P5：拍照图纸和高级修复

目标：

- 支持手机拍摄的纸质图纸。
- 提供人工辅助修复流程。

范围：

- 透视矫正。
- 旋转修正。
- 纸张背景和阴影校正。
- 局部低置信度重识别。
- 多页拼接。

任务拆解：

1. 自动检测纸张边缘。
2. 四角拖拽透视矫正。
3. 自动旋转和裁边。
4. 灰底 / 黄底纸张白平衡。
5. 局部格子重新采样。
6. 手动指定格子颜色。
7. 多页拼接排序。
8. 拼接后统一行列坐标。

验收标准：

- 拍照图纸可以通过手动四角矫正进入网格识别。
- 阴影不明显的照片能还原主要色块。
- 用户能手动修复低置信度区域。
- 多页拼接不会破坏坐标。

预计工作量：

- 5 - 10 个开发日。

## 9. 推荐版本切分

### v1：可用导入

包含：

- P0 基础导入闭环。
- P1 自有 JSON 回导。

交付标准：

- 用户可以导入结构化图纸。
- Dodoudou 图纸可以导出后再导入。
- 下游功能完整可用。

### v2：常见图片导入

包含：

- P2 清晰图片导入。
- 简单色块聚类。
- 手动区域和行列修正。

交付标准：

- 用户可以导入清晰截图。
- 系统不依赖 OCR 也能还原纯色块图纸。

### v3：色号级还原

包含：

- P3 图例 OCR。
- 品牌推断。
- 色号冲突确认。

交付标准：

- 导入后物料清单色号可信。
- 低置信度和冲突必须被用户确认。

### v4：PDF 和高级输入

包含：

- P4 PDF 导入。
- P5 拍照和高级修复中的核心能力。

交付标准：

- 官方 PDF、生成器 PDF、纸质图纸都有可用路径。

## 10. 验证计划

每个阶段都需要固定测试样本。

建议样本：

- Dodoudou JSON 小图纸。
- Dodoudou JSON 大图纸。
- CSV 纯色块图纸。
- 清晰截图 48x48。
- 清晰截图 100x100。
- 带图例截图。
- 带符号图例截图。
- 单页 PDF。
- 多页 PDF。
- 手机拍照图纸。

每次验收检查：

- 是否能生成 `PatternResult`。
- `width`、`height` 是否正确。
- `cells.length` 是否等于 `width * height` 或符合当前透明格策略。
- `palette.count` 是否和 cells 统计一致。
- `stats.totalCells` 是否正确。
- 是否能进入结果页。
- 是否能进入编辑器。
- 是否能下载。
- 是否能打开物料清单。
- 是否能进入专注模式。

## 11. 优先级边界

第一阶段不要做：

- 通用 OCR。
- PDF 解析。
- 拍照透视矫正。
- 多页拼接。
- 自动跨品牌替代。
- 自动扣库存。
- 把导入诊断写入 `PatternResult`。

第一阶段必须做：

- 标准 `PatternResult` 输出。
- 项目保存。
- 结果页跳转。
- 导入失败提示。
- validator。
- 可回归的样例数据。

## 12. 当前状态与下一步

更新时间：2026-06-08

### 12.1 P0 已完成

P0 基础导入闭环已经完成，图纸导入已从“开发中占位”变成可验证的基础功能。

已交付内容：

1. 新建 `src/lib/pattern-import/types.ts`，定义导入中间模型、诊断问题、校验报告和项目 import meta。
2. 新建 `src/lib/pattern-import/validate.ts`，实现 `validatePatternResult`，覆盖尺寸、cells、坐标、重复格、缺格、palette 数量差异等校验。
3. 新建 `src/lib/pattern-import/reconstruct.ts`，实现 `rebuildPatternPaletteFromCells`、统计重建和 `PatternResult` 归一化。
4. 新建 `src/lib/pattern-import/structured.ts`，支持 Dodoudou JSON、直接 `PatternResult` JSON、CSV / TSV 矩阵导入。
5. 新建 `src/lib/pattern-import/diagnostics.ts`，把基础导入诊断与项目 `importMeta` 分离，保持 `PatternResult` 干净。
6. 新建 `/workshop/import` 页面，支持文件选择、拖拽、品牌选择、解析结果预览、错误展示和保存为项目。
7. 首页“导入图纸”入口已跳转导入页。
8. `WorkshopProjectSourceType` 已增加 `'import'`，`WorkshopProjectRecord` 已增加可选 `importMeta`。
9. 导入成功后会创建 `kind: 'pattern'`、`status: 'ready'`、`viewMode: 'pattern'` 的项目，并跳转 `/workshop/result/:projectId`。
10. 结果页已支持没有原图但已有 `patternResult` 的导入项目预览和进入拼豆。
11. 已准备回归样例：`public/samples/pattern-import/dodoudou-heart.json`、`dodoudou-checker.json`、`color-blocks.csv`、`symbol-matrix.csv`、`symbol-matrix.tsv`。

已验证：

- `npm run check` 通过。
- 本地浏览器打开 `http://127.0.0.1:5173/workshop/import`，导入页可正常渲染。
- 首页“导入图纸”入口可跳转 `/workshop/import`。
- 导入页无浏览器控制台错误。

当前边界：

- 图片、PDF、OCR、网格识别仍未实现，导入页会给出阶段性提示。
- CSV / TSV 第一版按矩阵解析，每格支持 `#RRGGBB`、`transparent`、基础颜色名、当前品牌色号和普通 symbol token。
- symbol token 会被映射为稳定的临时颜色，后续 P2/P3 需要接入更完整的图例和色号确认流程。

### 12.2 P1 已完成

P1 自有图纸回导已经完成，v1“可用导入”版本形成完整闭环。当前采用旁路 `.dodoudou.json` 方案，暂不写 PNG metadata。

已交付内容：

1. 新建 `src/lib/pattern-import/dodoudou-json.ts`，定义 Dodoudou 自有导出 schema：`app`、`version`、`createdAt`、`patternResult`、`config`、`meta`。
2. `src/lib/pattern/download.ts` 已支持生成结构化 `.dodoudou.json`，文件内包含干净的 `PatternResult` 和当前下载配置。
3. `src/pages/workshop/DownloadSettingsModal.tsx` 已增加“可回导数据”开关；开启后下载 PNG 的同时下载 `.dodoudou.json`。
4. 结果页和编辑器下载入口均可传入当前 `config`，保证回导时能恢复品牌与画布尺寸配置。
5. `src/lib/pattern-import/structured.ts` 已识别 Dodoudou 自有 JSON schema version，不支持的版本会给出明确错误。
6. `/workshop/import` 保存项目时会优先使用自有 JSON 内的 `config`；CSV 和旧 JSON 继续使用导入页选择的默认色卡。
7. 回导后的项目仍保存为标准 `PatternResult`，导入诊断继续放在 `importMeta / analysis`，没有污染下游数据结构。

已验证：

- `npm run check` 通过。

当前边界：

- P1 第一版只做旁路 JSON，不做 PNG metadata、二维码或短码回导。
- `.dodoudou.json` 依赖用户主动在下载弹窗打开“可回导数据”开关。
- 自有 JSON 回导恢复的是图纸结构与配置；图片识别、PDF、OCR、网格对齐仍属于后续阶段。

### 12.3 P2 已启动

P2 清晰图片导入已完成前两批可用闭环：用户可以上传 PNG / JPG / WEBP，手动对齐网格，按红色分割网格采样，生成临时色号和标准 `PatternResult`，再在预览里做基础整理并保存为项目。

已交付内容：

1. 新增 `src/lib/pattern-import/image-decode.ts`，支持图片文件解码为 Canvas `ImageData`，并限制超大图片最大边长。
2. 新增 `src/lib/pattern-import/cell-read.ts`，按用户网格参数采样 cell 中心安全区域，使用主色桶降低格内文字和网格线干扰。
3. `cell-read.ts` 已实现基础颜色聚类，生成 `IMG01`、`IMG02` 等临时色号，并重建标准 `PatternResult`。
4. `/workshop/import` 已支持图片文件进入手动网格对齐流程，而不是直接提示 P2 未开放。
5. 导入页已绘制红色全图分割网格和绿色 3x3 / 6x6 校准网格。
6. 导入页已支持手动输入列数、行数、原点 X/Y、格宽和格高，可覆盖轻微非等比缩放截图。
7. 导入页已支持移动绿色校准区，便于检查不同局部区域的对齐效果。
8. 点击“下一步识别色块”后会生成预览、统计尺寸 / 豆数 / 颜色 / 空格，并可保存为项目。
9. 图片导入会记录低置信度格子数量到 validation / import meta，并在预览中用红色框高亮。
10. 预览图已支持点击格子改为当前选中的 `IMGxx` 临时色号。
11. 整理色号面板已支持把低置信度格子批量应用为当前色。
12. 整理色号面板已支持源色合并到目标色，用于修复压缩噪声导致的同色多簇。
13. 手动改色和颜色合并后会重新计算 `PatternResult.palette`、`stats`、validation 和低置信度计数。

已验证：

- `npm run check` 通过。

当前边界：

- 第一批 P2 以手动对齐为主，尚未实现自动网格检测。
- 当前仍是矩形网格模型，不支持明显透视变形。
- 当前手动改色以单格点击和批量低置信度为主，尚不支持框选多个格子。
- 颜色聚类会生成临时 `IMGxx` 色号，尚未映射到品牌色卡或 OCR 色号。
- PDF、OCR、图例 swatch 识别仍属于 P3/P4。

### 12.4 下一步推荐：继续 P2 第三批

下一批建议补齐“区域与重采样能力”，让截图类输入更稳定：

1. 增加选择区域 / 裁切框，避免整张手机截图或 UI 截图都参与网格对齐。
2. 增加一键按当前行列数适配图片区域，自动反推格宽 / 格高。
3. 增加一键重新采样，调整网格参数后无需重新上传图片。
4. 低置信度格子点击后展示采样色、聚类色和置信度。
5. 增加框选多个格子并批量改色。
6. 增加临时色号拆分工具，允许从现有 `IMGxx` 中分出新色。
7. 准备清晰 48x48、100x100、带格内色号截图样例，形成固定回归样本。
8. 补浏览器冒烟检查：图片上传、网格调整、下一步识别、手动改色、保存项目、结果页打开。
