# 嘟豆豆 ·「我的」底部导航与页面设计方案

> 目标：在现有底部导航「发现 / 工坊 / 画册」之后新增「我的」，承载个人资产、账号权益、同步状态与设置入口。页面设计遵循 `perler_beads_design_system.md` 的 Cozy Studio 视觉规范，保持温馨手工坊风格一致。

## 1. 设计结论

新增「我的」页不应该只是当前 `/account` 的平移，而应该成为个人中心与个人资产首页：

- 「画册」继续保留公共浏览属性：全部、最新、最热、公共详情。
- 「我的」承接个人资产：我的图纸、我的拼豆进度、我的发布、我的收藏。
- 「我的」承接账号能力：登录状态、账号资料、权益/额度、云同步、退出登录。
- 「我的」承接工具入口：库存、低库存提醒、导出记录、设置与帮助。
- 当前 `CollectionPage` 中的「我的」筛选建议逐步拆出，避免公共画册与个人资产混在同一导航语义下。

底部导航目标结构：

| 导航 | 职责 |
| --- | --- |
| 发现 | 今日入口、快捷创作、继续拼豆、灵感推荐 |
| 工坊 | 上传图片、空白画布、生成/编辑图纸的创作入口 |
| 画册 | 公共图纸浏览、官方/社区作品、收藏操作入口 |
| 我的 | 个人作品、拼豆进度、库存、权益、账号与设置 |

## 2. 现有功能盘点

### 2.1 已实现并适合迁入「我的」

| 功能 | 当前位置 | 当前能力 | 迁入方式 |
| --- | --- | --- | --- |
| 账号资料 | `src/pages/account/AccountPage.tsx` | 显示头像、昵称、邮箱/用户名、管理员入口、退出登录 | 作为「我的」页顶部账号卡 |
| 权益方案 | `AccountPage` + `EntitlementProvider` | 当前方案、云端作品额度、能力列表、匿名/Free/Admin 兜底 | 作为权益卡与额度进度 |
| 我的作品 | `src/pages/collection/CollectionPage.tsx?tab=my` | 最近打开、我的图纸、我的拼豆、收藏、本地作品同步提示 | 拆为「个人资产」主模块 |
| 我的发布 | `CollectionPage` + `fetchMyGalleryItems()` | 发布记录、审核状态：待审核/已发布/未通过/已下架 | 拆为「发布管理」模块 |
| 我的收藏 | `CollectionPage` + gallery favorites API | 本地收藏、登录后同步收藏 | 拆为「收藏图纸」模块 |
| 拼豆进度 | `listWorkshopProjects()` + `groupWorkshopProjects()` | progressing 项目、百分比、跳转专注拼豆 | 作为首页重点续做卡 |
| 库存管理 | `src/pages/beads/BeadInventoryPage.tsx` | 本地/云端库存、录入、修改、删除、搜索、品牌筛选、常用色、低库存阈值 | 作为「我的库存」入口与摘要 |
| 本地到云端同步 | `CollectionPage` / `BeadInventoryPage` | 作品和库存迁移、同步中状态、暂不处理 | 合并成「同步状态」模块 |
| 管理后台入口 | `AccountPage` | admin 用户可进入后台 | 放入设置列表，仅 admin 可见 |

### 2.2 应保留在原页面但由「我的」提供入口

| 功能 | 保留位置 | 「我的」中的入口 |
| --- | --- | --- |
| 图纸编辑器 | `/workshop/editor/:projectId` | 我的图纸卡片进入编辑 |
| 专注拼豆 | `/workshop/focus/:projectId` | 继续拼豆卡片进入 |
| 公共画册详情 | `/collection/:itemId` | 收藏/发布卡片进入 |
| 库存编辑完整页 | `/workshop/inventory` | 库存摘要卡进入 |
| 登录/注册/重置密码 | `/login` | 未登录态主按钮进入 |

### 2.3 当前可补的新功能

| 新功能 | 价值 | 数据来源/实现建议 |
| --- | --- | --- |
| 个人首页统计 | 一屏看清资产规模 | 作品数、进行中数、收藏数、库存色号数、低库存数 |
| 权益额度进度 | 让云同步限制透明 | `entitlements.limits.cloudProjects`、`cloudInventoryItems`，结合当前数量计算 |
| 同步健康状态 | 降低本地/云端混用不确定性 | 展示「本地」「云端」「有待同步」三种状态 |
| 最近继续 | 缩短回到上次项目路径 | 使用 `groupWorkshopProjects(projects).recent/progressing` |
| 发布审核中心 | 让社区发布闭环可见 | 展示待审核、已发布、未通过、已下架数量和列表 |
| 库存缺口提醒 | 将库存与图纸消耗关联 | 后续可基于最近图纸 requirements + inventory 计算缺口 |
| 下载/导出记录 | 方便回找已导出图纸 | 初期可做空状态和埋点预留，后续记录 `export.download` |
| 偏好设置 | 个性化默认品牌/默认画布/默认导出选项 | 初期本地设置，后续绑定账号 |
| 数据管理 | 用户可控地同步、清理本地缓存 | 管理 IndexedDB 本地作品、本地库存、登录后迁移 |

## 3. 信息架构

「我的」页面分为 5 个内容区，移动端单列纵向滚动，桌面端可在 `lg` 以上变成主内容 + 侧栏。

1. 账号与状态区
2. 快捷资产区
3. 继续创作区
4. 个人内容区
5. 设置与支持区

### 3.1 账号与状态区

内容：

- 头像、昵称、账号标识。
- 登录态文案：
  - 已登录：`Free / Admin`、邮箱或用户名。
  - 未登录：`游客模式`、提示「登录后可同步图纸、库存与收藏」。
- 主行动：
  - 未登录：`登录 / 注册`。
  - 已登录：`编辑资料`（新增预留）、`退出登录` 放在设置底部。
- 权益摘要：
  - 当前方案。
  - 云端作品额度：例如 `12 / 20`。
  - 云端库存额度：例如 `86 / 300`。
  - 可用能力提示：下载、发布、高清导出、无水印、AI 灵感。

### 3.2 快捷资产区

使用 2 × 2 网格卡片，承接高频入口：

| 卡片 | 主信息 | 次信息 | 点击 |
| --- | --- | --- | --- |
| 我的图纸 | 图纸总数 | 最近更新时间 | `/my/patterns` 或当前阶段跳 `/collection?tab=my#patterns` |
| 我的拼豆 | 进行中数量 | 最近进度百分比 | `/my/beading` 或最近项目 `/workshop/focus/:id` |
| 我的库存 | 色号数、低库存数 | 本地/云端状态 | `/workshop/inventory` |
| 我的收藏 | 收藏数量 | 已同步/本地收藏 | `/my/favorites` 或 `/collection?tab=my#favorites` |

首期如果不新增子路由，可在「我的」页内做锚点分区；二期再拆成 `/my/patterns`、`/my/inventory`、`/my/favorites`。

### 3.3 继续创作区

显示最需要回到现场的内容：

- 优先展示 `beadingState === 'progressing'` 的 1-2 个项目。
- 如果没有进行中项目，展示最近打开的图纸。
- 如果没有项目，展示空状态：
  - 标题：`还没有自己的图纸哦`
  - 说明：`上传一张图片，或从画册收藏一个图纸开始。`
  - 行动按钮：`开始创作`、`逛画册`

卡片信息：

- 封面/预览图。
- 标题。
- 图纸规格：`32×32 · 12 色 · 1024 颗`。
- 拼豆进度条：`68%`。
- 状态标签：`拼豆中` / `图纸` / `草稿` / `已完成`。

### 3.4 个人内容区

使用分段控件或横向 chip：

- `图纸`
- `拼豆`
- `发布`
- `收藏`

首屏默认推荐顺序：

1. 有进行中拼豆：默认 `拼豆`。
2. 有最近图纸：默认 `图纸`。
3. 有发布待处理：默认 `发布`。
4. 全空：默认 `图纸` 空状态。

#### 图纸

从 `groupWorkshopProjects(myItems).patterns` 读取：

- 草稿/图纸状态。
- 规格、颜色数、珠子数。
- 打开编辑器。
- 删除/重命名/复制作为后续操作预留。

#### 拼豆

从 `groupWorkshopProjects(myItems).progressing` 读取：

- 进度百分比。
- 最近更新。
- 继续拼豆。
- 标记完成作为后续操作预留。

#### 发布

从 `fetchMyGalleryItems()` 读取：

- 发布状态：待审核、已发布、未通过、已下架、草稿。
- 点击进入公共详情。
- 未通过原因当前数据模型没有，需要后续新增字段或后台接口。

#### 收藏

从 `syncFavoriteGalleryItems()` 或本地 `dodoudou.favoriteGalleryItemIds` 读取：

- 收藏图纸列表。
- 点击进入画册详情。
- 取消收藏。
- 未登录时显示本地收藏；登录后提示同步。

### 3.5 设置与支持区

列表分组：

| 分组 | 条目 | 状态 |
| --- | --- | --- |
| 账号 | 编辑资料、账号安全、退出登录 | 编辑资料/安全为新增 |
| 创作偏好 | 默认品牌、默认画布尺寸、默认导出样式 | 新增 |
| 数据 | 同步本地作品、同步本地库存、清理本地缓存 | 部分已有 |
| 权益 | 当前方案、用量明细、升级方案 | 升级方案为新增 |
| 支持 | 帮助中心、反馈问题、关于嘟豆豆 | 新增 |
| 管理 | 后台管理 | 已有 admin 条件显示 |

## 4. 页面线框

### 4.1 移动端结构

```text
┌─────────────────────────┐
│ 我的                    │
│ ┌─────────────────────┐ │
│ │ 头像  昵称/游客      │ │
│ │       方案 · 同步状态 │ │
│ │ [登录/编辑资料]       │ │
│ └─────────────────────┘ │
│                         │
│ ┌──────┐ ┌──────┐      │
│ │图纸  │ │拼豆  │      │
│ │12    │ │2 个  │      │
│ └──────┘ └──────┘      │
│ ┌──────┐ ┌──────┐      │
│ │库存  │ │收藏  │      │
│ │86色  │ │18张  │      │
│ └──────┘ └──────┘      │
│                         │
│ 继续创作                │
│ ┌─────────────────────┐ │
│ │ 预览图  标题         │ │
│ │        32×32 · 12色 │ │
│ │        █████░ 68%   │ │
│ └─────────────────────┘ │
│                         │
│ 图纸  拼豆  发布  收藏  │
│ ┌──────┐ ┌──────┐      │
│ │卡片  │ │卡片  │      │
│ └──────┘ └──────┘      │
│                         │
│ 设置与支持              │
│ 账号安全       ›        │
│ 创作偏好       ›        │
│ 数据同步       ›        │
└─────────────────────────┘
```

### 4.2 桌面/平板结构

`md` 以上保持内容最大宽度，`lg` 以上变为两列：

- 左列：账号卡、权益卡、设置列表。
- 右列：快捷资产、继续创作、个人内容列表。

```text
┌─────────────────────────────────────────┐
│ 我的                                    │
│ ┌──────────────┐ ┌────────────────────┐ │
│ │ 账号卡        │ │ 快捷资产 2×2       │ │
│ │ 权益额度      │ └────────────────────┘ │
│ │ 设置与支持    │ ┌────────────────────┐ │
│ └──────────────┘ │ 继续创作            │ │
│                  └────────────────────┘ │
│                  ┌────────────────────┐ │
│                  │ 图纸/拼豆/发布/收藏 │ │
│                  └────────────────────┘ │
└─────────────────────────────────────────┘
```

## 5. 视觉设计

### 5.1 风格原则

基于 `perler_beads_design_system.md`：

- 主背景：`--c-bg #FDFBF7`。
- 卡片背景：`--c-surface #FFFFFF`。
- 主行动/激活态：`--c-primary #D8B4E2`。
- 成功/同步完成：`--c-mint #B5EAD7`。
- 提醒/低库存：`--c-orange #FFDAC1`。
- 危险/退出/错误：`Rose #F4B8BB`。
- 标题使用 `--font-title`，正文使用 `--font-body`。
- 页面整体保持柔和、手作、拼豆颗粒感，避免商业后台式冷硬信息密度。

### 5.2 顶部账号卡

样式：

- 大圆角 `24px`。
- 背景：白色叠加非常浅的薰衣草/薄荷渐变。
- 左侧头像：64px 圆形，未登录时显示 `D` 或 logo 图形。
- 右侧标题：昵称/游客模式。
- 状态 badge：
  - 已登录：`云端同步中` / `已同步`。
  - 未登录：`本地保存`。
  - 管理员：`Admin`。

文案示例：

- 已登录：`欢迎回来，Fei`
- 未登录：`游客创作中`
- 辅助文案：`登录后可同步图纸、库存与收藏`

### 5.3 快捷资产卡

样式：

- 2 列网格。
- 卡片圆角 `20px`，轻阴影。
- 每张卡使用不同点缀色，避免单一紫色：
  - 图纸：Primary Light。
  - 拼豆：Mint。
  - 库存：Peach。
  - 收藏：Sky/Rose 淡色。
- 数字为主视觉，标题为辅助。
- 使用拼豆小圆点或色块 swatch 做装饰，但装饰不能影响点击区域。

### 5.4 继续创作卡

样式：

- 横向卡片，左图右文。
- 预览图 `aspect-ratio: 1 / 1`，`image-rendering: pixelated`。
- 进度条使用 Mint 填充，底色为浅边框。
- 进度状态同时用文字表达，不只依靠颜色。

### 5.5 个人内容列表

图纸/收藏使用当前 `collection-my-card` 风格：

- 圆角 `20px`。
- 白色卡面。
- 图片居中 contain。
- badge 使用 `is-mine` / `is-saved` / `is-beading`。

拼豆进度使用当前 `collection-progress-item` 风格：

- 横向列表。
- 进度百分比右侧固定宽度，避免布局跳动。
- 卡片点击进入专注拼豆。

发布管理增加状态色：

| 状态 | Badge |
| --- | --- |
| 待审核 | Peach |
| 已发布 | Mint |
| 未通过 | Rose |
| 已下架 | Text Hint |
| 草稿 | Primary Light |

### 5.6 设置列表

样式：

- 不做重卡片堆叠，使用白色分组面板。
- 每个列表项最小高度 52px。
- 左侧图标，中心标题/说明，右侧箭头或状态。
- 危险操作「退出登录」使用 Rose 文本，不放在顶部。

## 6. 状态设计

### 6.1 未登录

展示：

- 顶部账号卡为游客模式。
- 快捷资产仍展示本地作品/本地库存/本地收藏数量。
- 同步相关位置展示登录提示。
- 主按钮：`登录并同步`。
- 次按钮：`继续游客使用`。

注意：

- 不阻塞本地创作。
- 不暗示游客数据已云端备份。

### 6.2 已登录

展示：

- 账号信息。
- 当前方案。
- 云端额度。
- 本地待迁移作品/库存提示。
- 收藏同步结果。

同步提示优先级：

1. 同步失败：Rose 提示，可重试。
2. 有本地待同步：Peach 提示，展示数量。
3. 正在同步：loading 状态。
4. 已同步：Mint 提示。

### 6.3 空状态

遵循设计系统空状态规则：

- 图标：`🧵` 或手作相关轻插图。
- 标题：快乐体。
- 虚线边框使用 Primary Light。
- 至少一个行动按钮。

示例：

- 我的图纸空：`还没有图纸哦` / `上传一张图片，或从画册收藏一个喜欢的图纸。` / `开始创作`
- 我的库存空：`还没有录入库存` / `录入常用色号后，图纸会自动检查缺口。` / `录入库存`
- 我的发布空：`还没有发布作品` / `完成图纸后，可以上传到公共画册。` / `去工坊`

### 6.4 加载状态

- 账号/权益加载：账号卡内骨架屏，不整页阻塞。
- 作品列表加载：局部 skeleton 卡片。
- 同步中：按钮文案 `同步中...`，禁用重复点击。
- 读取失败：模块内错误提示，保留其它模块可用。

## 7. 导航与路由建议

### 7.1 首期最小改动

新增：

- `NavItemId`: 增加 `'my'`。
- `navItems`: 增加 `{ id: 'my', label: '我的', iconSrc, activeIconSrc }`。
- 路由：`/my`。
- 页面：`src/pages/my/MyPage.tsx`。

保留：

- `/account` 暂不删除，可重定向到 `/my` 或仅作为老入口。
- `/collection?tab=my` 暂时保留，避免深链失效。
- `/workshop/inventory` 继续作为库存完整管理页。

底部点击：

```ts
tab === 'discovery' ? '/discovery' : tab === 'my' ? '/my' : `/${tab}`
```

### 7.2 二期子路由

可选新增：

- `/my/patterns`
- `/my/beading`
- `/my/published`
- `/my/favorites`
- `/my/settings`

如果首期资源有限，先不要拆子路由，避免状态管理重复。

### 7.3 Icon 资源

当前已有：

- `/assets/system_icons/my.png`
- `/assets/system_icons/my_active.png`

底部导航当前使用 `/assets/icons/*`，建议新增同规格：

- `/assets/icons/my_inactive.svg`
- `/assets/icons/my_active.svg`

如果先复用 png，也需要确认 24px 下清晰度和 active 颜色一致。

## 8. 数据与接口

### 8.1 首期复用数据

| 数据 | 来源 |
| --- | --- |
| 用户信息 | `useAuth()` |
| 权益 | `useEntitlements()` |
| 作品 | `listWorkshopProjects()` |
| 作品分组 | `groupWorkshopProjects()` |
| 本地作品 | `listLocalWorkshopProjects()` |
| 云端作品同步 | `syncRemoteWorkshopProjects()` |
| 我的发布 | `fetchMyGalleryItems()` |
| 收藏同步 | `syncFavoriteGalleryItems()` |
| 库存 | `listInventoryItems()` / `listRemoteInventoryItems()` |
| 库存同步 | `syncRemoteInventoryItems()` |

### 8.2 需要补齐的数据能力

| 能力 | 建议 |
| --- | --- |
| 账号资料编辑 | 新增 `/api/auth/profile` 或 `/api/user/me` PATCH |
| 发布未通过原因 | GalleryItem 增加 reviewReason 或后台接口返回审核备注 |
| 导出记录 | 新增 ExportHistory 表或前端本地记录 |
| 默认偏好 | 新增 UserPreference 表或 localStorage 首期实现 |
| 统一资产统计 | 可前端聚合，后续做 `/api/my/summary` 减少请求 |

## 9. 组件拆分建议

首期可在 `src/pages/my` 下拆分：

```text
src/pages/my/
  MyPage.tsx
  MyPage.module.css
  components/
    MyProfileCard.tsx
    MyAssetGrid.tsx
    MyContinueCard.tsx
    MyLibraryTabs.tsx
    MySyncPanel.tsx
    MySettingsList.tsx
```

可复用现有逻辑：

- 从 `CollectionPage` 抽出我的作品数据加载逻辑，避免复制过多 `useEffect`。
- 从 `AccountPage` 迁出账号卡和退出登录逻辑。
- 库存页继续独立，`MyPage` 只读取摘要，不放完整录入表单。

## 10. 开发分期

### Phase 1：导航与聚合页

- 新增底部导航「我的」。
- 新增 `/my` 页面。
- 顶部账号卡、权益卡。
- 快捷资产统计。
- 继续创作卡。
- 设置列表入口。
- `/account` 跳转或保留为兼容页面。

### Phase 2：拆出个人资产

- 将 `CollectionPage` 的「我的」tab 内容迁到「我的」页。
- `CollectionPage` 移除或弱化「我的」筛选，只保留公共筛选。
- 增加「发布」「收藏」分段。
- 增加同步状态统一提示。

### Phase 3：设置与新增能力

- 账号资料编辑。
- 创作偏好。
- 导出记录。
- 数据管理。
- 发布审核原因展示。
- 权益升级入口。

## 11. 验收标准

- 底部导航显示 4 个 tab：发现、工坊、画册、我的。
- 点击「我的」进入 `/my`，底部 active 状态正确。
- 未登录状态能看到游客模式、本地资产和登录引导。
- 已登录状态能看到账号、权益、云端作品/库存额度。
- 我的图纸、我的拼豆、我的发布、我的收藏至少能展示已有数据或空状态。
- 库存入口能进入 `/workshop/inventory`。
- 管理员账号能看到后台管理入口，普通用户不可见。
- 页面在 `xs/sm/md/lg/xl` 断点无内容重叠，底部导航不遮挡最后一项。
- 所有交互点击区域不小于 44×44px，键盘 focus 可见。
- 不破坏现有 `/collection?tab=my`、`/account`、`/workshop/inventory` 深链。

## 12. 关键取舍

- 「我的」页首期做聚合，不在一个页面塞完整库存编辑表单，避免信息过载。
- 「画册」的「我的」tab 应逐步迁出，因为底部导航已有「我的」后，公共画册语义会更清晰。
- 云同步提示需要集中展示，减少用户在画册、库存、账号页反复看到不同同步状态。
- 个人页必须兼容游客模式，当前产品已经支持本地作品和本地库存，不能强制登录。

## 13. 开发计划

### 13.1 Phase 1 交付范围

本轮先完成可进入、可浏览、可跳转的「我的」首期聚合页：

1. 底部导航新增「我的」。
2. 新增 `/my` 路由，并让底部导航 active 状态正确。
3. 新增 `MyPage`，聚合账号、权益、作品、拼豆进度、发布、收藏、库存摘要。
4. 未登录时展示游客模式、本地资产和登录引导。
5. 已登录时展示云端权益、本地待同步提示、后台管理入口和退出登录。
6. 复用现有完整页面：库存仍进入 `/workshop/inventory`，图纸进入编辑器，拼豆进入专注模式，发布/收藏进入画册详情。
7. 保留 `/account` 和 `/collection?tab=my` 深链，不在本轮删除旧入口。

### 13.2 文件改动

| 文件 | 改动 |
| --- | --- |
| `src/app/navigation.ts` | `NavItemId` 增加 `my`，底部导航增加「我的」 |
| `src/app/routes.ts` | `AppRoute` 和 `routePathMap` 增加 `my` |
| `src/app/App.tsx` | 引入 `MyPage`，新增 `/my` 路由和 tab 映射 |
| `src/pages/discovery/DiscoveryPage.tsx` | 登录头像入口从 `/account` 调整到 `/my` |
| `src/pages/my/MyPage.tsx` | 新增页面，实现数据聚合、入口跳转和状态展示 |
| `src/styles/global.css` | 新增 `my-*` 样式，复用现有 token 和卡片语言 |

### 13.3 实现顺序

1. 更新导航与路由。
2. 新建 `MyPage` 的数据加载骨架。
3. 加入账号卡、资产快捷卡、继续创作、个人内容分段、设置列表。
4. 补充同步动作：作品同步、库存同步、收藏同步。
5. 写样式并检查移动端/桌面端布局。
6. 运行 `npm run check` 或 `npm run build` 验证类型与构建。
7. 启动本地页面，用浏览器检查 `/my` 和底部导航。

### 13.4 暂缓到 Phase 2

- 从 `CollectionPage` 完全移除「我的」tab。
- 新增 `/my/patterns`、`/my/beading`、`/my/published`、`/my/favorites` 子路由。
- 账号资料编辑、创作偏好、导出记录、审核未通过原因。
- 独立 `/api/my/summary` 聚合接口。
