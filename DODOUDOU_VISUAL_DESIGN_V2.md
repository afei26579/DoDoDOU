# 嘟豆豆 Dodoudou · 视觉设计规范 v2

Perler Beads Creator App  
Design System v2.0 · Cozy Craft Studio + Focused Editor

> 本版本基于现有三色 3D 拼豆 DDD Logo 与原 `perler_beads_design_system.md` 进行优化。v2 的重点不是继续增加可爱装饰，而是把品牌温度、移动端效率、拼豆编辑器可用性统一到一套可落地的视觉系统里。

## 目录

- [00 · 设计定位](#00--设计定位)
- [01 · 品牌 Logo 系统](#01--品牌-logo-系统)
- [02 · 色彩系统](#02--色彩系统)
- [03 · 字体与排版](#03--字体与排版)
- [04 · 移动端布局原则](#04--移动端布局原则)
- [05 · 圆角与阴影](#05--圆角与阴影)
- [06 · 核心组件](#06--核心组件)
- [07 · 页面视觉方向](#07--页面视觉方向)
- [08 · 动效规范](#08--动效规范)
- [09 · 空状态与插画](#09--空状态与插画)
- [10 · 无障碍与可用性](#10--无障碍与可用性)
- [11 · CSS Token 摘要](#11--css-token-摘要)
- [12 · 落地检查清单](#12--落地检查清单)

## 00 · 设计定位

Dodoudou 不是单纯的可爱工具，也不是冷冰冰的图片转换器。它应该是一个温柔、有陪伴感，但足够清晰专业的拼豆创作工作台。

核心产品路径：

```text
灵感发现 → 图片上传 / AI 生成 → 图纸生成 → 编辑调整 → 下载导出 → 专注拼豆
```

视觉关键词：

| 关键词 | 含义 |
| --- | --- |
| 软糯 | 用于品牌、插画、空状态、完成反馈 |
| 清晰 | 用于编辑器、参数、颜色、导出流程 |
| 专注 | 用于拼豆制作模式、画布、工具栏 |
| 手作 | 用于珠子纹理、格子、色板、材料清单 |
| 轻量 | 移动端优先，不堆卡片，不做重装饰 |

设计原则：

1. 首页可以可爱，编辑器必须清晰。
2. 浅色负责情绪，深色负责操作。
3. 画布是核心，不让装饰抢走注意力。
4. 色彩、状态、数量、进度必须能被快速扫读。
5. 所有可爱元素都要服务创作流程，而不是只做氛围。

## 01 · 品牌 Logo 系统

### Logo 概念

现有 3 个 D 的拼豆 Logo 概念保留，代表：

| 符号 | 含义 |
| --- | --- |
| Dodo | 品牌名记忆点 |
| Dou | 豆子、珠子、拼豆 |
| Do | 动手创作 |

3 个 D 的形式与 Dodoudou 名称有天然关联，三色珠子也能直接传达拼豆材料感。这个方向成立，但需要从一张 3D 品牌图扩展成完整 Logo 系统。

### Logo 分层

| 类型 | 用途 | 说明 |
| --- | --- | --- |
| 3D Logo | 启动页、官网首屏、品牌宣传图 | 保留高光、珠子质感、真实拼豆感觉 |
| Flat Logo | App 图标、导航栏、按钮内图标 | 去掉复杂高光，强化轮廓和孔洞 |
| Mono Logo | 深浅背景、印刷、水印 | 单色版本，保证极小尺寸可识别 |
| Wordmark | 早期品牌传播 | 建议搭配“嘟豆豆 Dodoudou”文字使用 |

### Logo 使用建议

- 不要只使用 3D 版本作为全场景 Logo。
- 3D 版本适合大尺寸展示，不适合 24px、32px、favicon、底部导航等小尺寸场景。
- App 图标建议使用 Flat Logo，并强化整体外轮廓。
- 早期品牌传播建议使用图形 + `嘟豆豆 Dodoudou` 的组合锁定识别。
- 深色背景、浅色背景、单色印刷都需要独立测试。

### Logo 色彩

```css
--logo-purple: #B77BE3;
--logo-mint: #6CCFB0;
--logo-peach: #F59A73;
```

Logo 三色可以比 UI pastel 更饱和，保证品牌识别；UI 中的浅色版本则用于背景和情绪表达。

## 02 · 色彩系统

### 色彩策略

原方案的 pastel 色彩很亲和，但对比度不足。v2 调整为：

```text
浅色负责情绪，深色负责操作。
```

浅紫、薄荷、蜜桃继续作为品牌基因保留，但不直接承担主按钮文字、重要状态文字、编辑器关键操作。

### Core Palette

| Token | 色值 | 用途 |
| --- | --- | --- |
| `--c-bg` | `#FDFBF7` | 全局背景，温暖纸感 |
| `--c-surface` | `#FFFFFF` | 卡片、面板 |
| `--c-surface-soft` | `#FFF7F0` | 次级区域、空状态 |
| `--c-text` | `#4F463F` | 主标题、正文 |
| `--c-text-muted` | `#746960` | 辅助文字 |
| `--c-text-hint` | `#9B9087` | placeholder、弱提示 |
| `--c-border` | `rgba(79, 70, 63, 0.14)` | 默认边框 |
| `--c-border-strong` | `rgba(79, 70, 63, 0.28)` | 画布、表格、分割线 |

### Brand Palette

| Token | 色值 | 用途 |
| --- | --- | --- |
| `--c-primary` | `#8F5DB7` | 主按钮、当前步骤、关键操作 |
| `--c-primary-hover` | `#76469F` | 按下 / hover |
| `--c-primary-soft` | `#EDD9F5` | 选中背景、轻量标签 |
| `--c-primary-tint` | `#F7EEFB` | 极轻品牌背景 |
| `--c-mint` | `#4FAF95` | 成功、上传、完成 |
| `--c-mint-soft` | `#DDF7ED` | 成功背景 |
| `--c-peach` | `#D9784F` | 提醒、注意、材料不足 |
| `--c-peach-soft` | `#FFE4D2` | 提醒背景 |
| `--c-rose` | `#C95C68` | 错误、删除、失败 |
| `--c-rose-soft` | `#FFE1E5` | 错误背景 |
| `--c-sky` | `#4F8DCB` | 信息、AI、帮助 |
| `--c-sky-soft` | `#E3F0FF` | 信息背景 |

### 拼豆功能色

| Token | 色值 | 用途 |
| --- | --- | --- |
| `--grid-line` | `rgba(79, 70, 63, 0.18)` | 默认格线 |
| `--grid-line-strong` | `rgba(79, 70, 63, 0.36)` | 大分割线、当前区域 |
| `--grid-active` | `#8F5DB7` | 当前格子 |
| `--grid-row-active` | `rgba(237, 217, 245, 0.72)` | 当前行 |
| `--grid-completed` | `rgba(79, 70, 63, 0.32)` | 已完成标记 |
| `--canvas-bg` | `#FFFDF9` | 画布背景 |
| `--canvas-empty` | `#F0ECE6` | 空格 / 透明背景 |

### 使用规则

- 主按钮用深紫 `--c-primary`，不用浅紫。
- 浅紫只做背景、选中态、轻提示。
- 成功、警告、错误、信息都使用深色 token 承担文字和图标。
- 编辑器界面减少大面积 pastel，避免干扰画布颜色判断。
- 色彩不单独传递状态，必须搭配文字、图标或形状。

## 03 · 字体与排版

### 字体策略

`ZCOOL KuaiLe` 很有品牌感，但不适合大量 UI 信息。v2 的字体策略是：

| 场景 | 字体 |
| --- | --- |
| Logo / 欢迎语 / 空状态标题 | `ZCOOL KuaiLe` |
| 中文 UI / 正文 / 参数 | `PingFang SC`, `Microsoft YaHei`, `Noto Sans SC`, system-ui |
| 英文数字 / 数量 / 参数 | `Nunito`, system-ui, sans-serif |

### 字体层级

| 层级 | 规格 | 用途 |
| --- | --- | --- |
| Display | 28px / 1.25 / 品牌字体 | 启动页、欢迎页 |
| H1 | 24px / 1.35 / 700 | 页面主标题 |
| H2 | 18px / 1.45 / 700 | 面板标题 |
| H3 | 16px / 1.5 / 700 | 分组标题 |
| Body | 15px / 1.65 / 400 | 正文说明 |
| Body Small | 13px / 1.5 / 400 | 参数说明、辅助信息 |
| Caption | 12px / 1.45 / 500 | 标签、数量、状态 |
| Number | 14px / 1.2 / 700 | 珠子数、尺寸、进度 |

### 排版规则

- 品牌字体只用于情绪表达，不进入复杂参数区。
- 编辑器、下载、材料清单优先使用清晰 UI 字体。
- 移动端正文不低于 13px，主要操作不低于 15px。
- 数字信息需要更高字重，便于扫读。
- 不使用负字距，不使用随视口宽度缩放的字体。

## 04 · 移动端布局原则

Dodoudou 是移动端优先，不应做成桌面卡片缩小版。

### 核心布局

| 区域 | 说明 |
| --- | --- |
| Top Bar | 返回、标题、保存状态、更多 |
| Canvas Area | 最大优先级，图纸 / 原图 / 对比视图 |
| Bottom Tool Bar | 画笔、橡皮、吸管、填充、撤销 |
| Bottom Sheet | 色板、参数、导出、材料清单 |
| Focus Mode | 拼豆时隐藏非必要 UI，只保留行列进度 |

### 页面节奏

- 首页可以使用大圆角、插画、柔和色块。
- 上传和生成流程可以有插画和轻说明。
- 编辑器必须克制，画布优先。
- 专注拼豆模式应接近制作仪表盘，不做花哨装饰。
- 卡片只能承载独立内容，不做层层嵌套。

### 移动端断点

| 断点 | 范围 | 布局说明 |
| --- | --- | --- |
| xs | `< 360px` | 极小屏，工具栏图标优先，文字压缩 |
| sm | `360-599px` | 手机，单列布局，Bottom Sheet 为主 |
| md | `600-959px` | 平板，画布 + 侧面板 |
| lg | `960-1279px` | 桌面，画布 + 双侧栏 |
| xl | `>= 1280px` | 宽屏，最大内容宽度 1440px |

## 05 · 圆角与阴影

### 圆角

| Token | 值 | 用途 |
| --- | --- | --- |
| `--r-xs` | `6px` | 拼豆格子、色块 |
| `--r-sm` | `10px` | 输入框、标签 |
| `--r-md` | `14px` | 按钮、小面板 |
| `--r-lg` | `20px` | 卡片、Bottom Sheet |
| `--r-xl` | `28px` | 欢迎页大模块 |
| `--r-full` | `999px` | pill、头像、进度点 |

### 阴影

编辑器里减少阴影，避免影响画布判断。品牌页和首页可以更柔和。

```css
--shadow-soft: 0 2px 10px rgba(79, 70, 63, 0.06);
--shadow-card: 0 6px 24px rgba(143, 93, 183, 0.12);
--shadow-float: 0 12px 36px rgba(79, 70, 63, 0.16);
--shadow-button: 0 3px 12px rgba(143, 93, 183, 0.28);
```

### 使用规则

- 首页和空状态可以使用 `--shadow-card`。
- 编辑器固定工具栏优先用边线和背景分层，不依赖重阴影。
- Bottom Sheet 使用 `--shadow-float`，但透明度要克制。
- 大面积容器圆角不超过 28px，功能卡片优先 14-20px。

## 06 · 核心组件

### 6.1 拼豆画布 Bead Canvas

拼豆画布是产品最重要的组件，优先级高于装饰、卡片和品牌插画。

状态：

| 状态 | 表现 |
| --- | --- |
| 默认格子 | 细边框，低对比 |
| 当前格子 | 深紫描边 + 轻微放大 |
| 当前行 | 横向淡紫背景 |
| 已完成格子 | check 标记或透明度变化 |
| 被编辑格子 | 80ms scale 反馈 |
| 空格子 | 透明棋盘格 |
| 错误格子 | 红色描边，不改变原颜色 |
| 色弱辅助 | 允许显示编号、符号或纹理 |

交互要求：

- 支持双指缩放。
- 支持拖动画布。
- 支持网格开关。
- 支持行列标尺。
- 支持当前格子键盘方向键导航。
- 支持撤销 / 重做。

### 6.2 色板 Color Palette

色板不只是颜色展示，要支持真实制作。

每个色块包含：

- 颜色
- 编号 / 色号
- 珠子数量
- 是否已完成
- 是否被替换
- 是否缺货或建议购买

状态：

| 状态 | 表现 |
| --- | --- |
| 默认 | 色块 + 数量 |
| 当前选中 | 深紫外框 |
| 已完成 | check 图标 |
| 数量不足 | 蜜桃提示 |
| 被替换 | 双层色块 |
| 隐藏 | 降低透明度，保留可恢复入口 |

### 6.3 上传区 Upload Zone

上传区可以保留手作感，但不要只靠虚线框。

入口：

| 入口 | 用途 |
| --- | --- |
| 上传图片 | 主路径 |
| AI 生成 | 无素材用户 |
| 示例图案 | 降低新用户门槛 |
| 最近作品 | 快速继续 |

主 CTA：

```text
上传图片
```

次 CTA：

```text
让 AI 生成
```

### 6.4 参数面板 Pattern Settings

关键参数：

| 参数 | 选项 |
| --- | --- |
| 尺寸 | 小 / 中 / 大 / 自定义 |
| 拼板类型 | 方形 / 圆形 / 六边形 |
| 色彩数量 | 8 / 16 / 32 / 自定义 |
| 背景处理 | 保留 / 移除 / 简化 |
| 难度预估 | 简单 / 普通 / 复杂 |

设计重点：

- 参数要像调节工具，不像表单问卷。
- 重要结果需要即时预览。
- 每个参数需要说明影响：珠子数量、复杂度、预计时间。

### 6.5 Bottom Tool Bar

移动端编辑器的底部工具栏建议包含：

| 工具 | 图标优先 | 说明 |
| --- | --- | --- |
| 画笔 | 是 | 修改单格颜色 |
| 橡皮 | 是 | 清除格子 |
| 吸管 | 是 | 从图纸取色 |
| 填充 | 是 | 区域填色 |
| 移动 | 是 | 拖动画布 |
| 撤销 | 是 | 回退操作 |
| 重做 | 是 | 恢复操作 |

规则：

- 工具栏按钮保持 44x44px 最小触控区域。
- 当前工具使用深紫描边或背景。
- 不用长文字挤占工具栏，可用 tooltip 或长按说明。

### 6.6 专注拼豆模式 Focus Mode

这是产品差异化重点。专注模式不是编辑器换皮，而是制作过程的陪伴面板。

界面只保留：

- 当前行 / 当前列
- 当前颜色
- 剩余珠子数
- 上一步 / 下一步
- 完成当前颜色
- 防息屏提示

专注模式色彩：

```css
--focus-bg: #2E2925;
--focus-surface: #3A332E;
--focus-text: #FFF8F0;
--focus-muted: #D8CDC3;
--focus-accent: #B5EAD7;
--focus-warning: #FFBE8E;
```

专注模式规则：

- 支持大字模式。
- 支持左手模式。
- 支持防误触锁定画布。
- 支持深色背景，降低长时间制作疲劳。
- 保留完成反馈，但不要频繁打断。

## 07 · 页面视觉方向

### 7.1 首页

目标：建立亲和感和创作入口。

内容优先级：

1. 品牌 Logo
2. 最近项目
3. 上传 / AI 生成
4. 灵感图案
5. 教程或模板

视觉规则：

- 可以使用大圆角、插画、柔和色块。
- 首屏必须有明确创作入口。
- 不要让品牌装饰压过“开始创作”。

### 7.2 上传生成页

目标：让用户知道下一步会发生什么。

重点展示：

- 支持格式
- 推荐图片类型
- 生成预览
- 参数入口
- 预计难度

避免大段说明，使用小提示卡和即时反馈。

### 7.3 编辑器页

目标：效率第一。

布局：

| 区域 | 内容 |
| --- | --- |
| 顶部 | 返回、项目名、保存状态、更多 |
| 中间 | 画布 |
| 底部 | 工具栏 |
| 抽屉 | 色板 / 参数 / 导出 |

编辑器中，品牌色只用于当前状态和关键操作。

### 7.4 下载导出页

目标：让用户放心拿去制作。

导出内容：

- 图纸 PNG / PDF
- 材料清单
- 色号表
- 分步拼豆指南
- 分享图

导出页要强调结果可信，不要只做一个下载按钮。

### 7.5 专注制作页

目标：帮助用户实际完成作品。

优先级：

1. 当前需要放哪种珠子
2. 放在哪一行 / 哪一列
3. 还剩多少
4. 下一步是什么

这个页面应比首页更沉稳，比编辑器更少控件。

## 08 · 动效规范

### 动效原则

动效表达手作生成感，但不能拖慢效率。

| 类型 | 时长 | 用途 |
| --- | --- | --- |
| 点击反馈 | 80ms | 按钮、格子 |
| 面板切换 | 180ms | Bottom Sheet、Tab |
| Toast | 220ms | 成功、失败 |
| 页面切换 | 260ms | 流程页 |
| 图纸生成 | 最长 900ms | 格子填充，不按总格数无限增长 |

### 拼豆生成动效

```text
总格数 <= 400：逐格 stagger
总格数 401-1600：按行 stagger
总格数 > 1600：按区域渐显
最大动效时长：900ms
```

### 减弱动效

检测到 `prefers-reduced-motion: reduce` 时：

- 取消 scale、弹性、位移动效。
- 保留必要 opacity 变化。
- 图纸生成直接显示结果或使用极短淡入。

## 09 · 空状态与插画

### 空状态策略

不要过度依赖 emoji。建议建立 Dodoudou 自有小插画系统：

- 小拼板
- 散落珠子
- 镊子
- 熨斗纸
- 半完成图案
- 迷你 D 珠子形象

### 标准空状态

```text
标题：还没有图纸
说明：上传一张图片，嘟豆豆会帮你转换成可以直接制作的拼豆图纸。
按钮：开始创作
```

### 空状态规则

- 必须提供至少一个行动按钮。
- 标题可以使用品牌字体。
- 说明文字使用清晰 UI 字体。
- 插画保持轻量，不要占据过多首屏高度。

## 10 · 无障碍与可用性

必须支持：

| 规范 | 级别 |
| --- | --- |
| 正文对比度 >= 4.5:1 | 必须 |
| 触控区域 >= 44x44px | 必须 |
| 所有控件支持 `:focus-visible` | 必须 |
| 状态不只靠颜色表达 | 必须 |
| 画布支持键盘方向键 | 推荐 |
| 当前格子有清晰高亮 | 必须 |
| 色板提供色名 / 色号 / 数量文本 | 必须 |
| 支持减弱动效 | 必须 |
| 专注模式支持大字模式 | 推荐 |
| 支持左手模式 | 推荐 |
| 支持色弱友好编号或符号 | 推荐 |

Focus ring：

```css
--focus-ring: 0 0 0 3px rgba(143, 93, 183, 0.36);
--focus-ring-offset: 2px;
```

## 11 · CSS Token 摘要

```css
:root {
  --c-bg: #FDFBF7;
  --c-surface: #FFFFFF;
  --c-surface-soft: #FFF7F0;

  --c-text: #4F463F;
  --c-text-muted: #746960;
  --c-text-hint: #9B9087;

  --c-border: rgba(79, 70, 63, 0.14);
  --c-border-strong: rgba(79, 70, 63, 0.28);

  --c-primary: #8F5DB7;
  --c-primary-hover: #76469F;
  --c-primary-soft: #EDD9F5;
  --c-primary-tint: #F7EEFB;

  --c-mint: #4FAF95;
  --c-mint-soft: #DDF7ED;
  --c-peach: #D9784F;
  --c-peach-soft: #FFE4D2;
  --c-rose: #C95C68;
  --c-rose-soft: #FFE1E5;
  --c-sky: #4F8DCB;
  --c-sky-soft: #E3F0FF;

  --logo-purple: #B77BE3;
  --logo-mint: #6CCFB0;
  --logo-peach: #F59A73;

  --grid-line: rgba(79, 70, 63, 0.18);
  --grid-line-strong: rgba(79, 70, 63, 0.36);
  --grid-active: #8F5DB7;
  --grid-row-active: rgba(237, 217, 245, 0.72);
  --grid-completed: rgba(79, 70, 63, 0.32);
  --canvas-bg: #FFFDF9;
  --canvas-empty: #F0ECE6;

  --focus-bg: #2E2925;
  --focus-surface: #3A332E;
  --focus-text: #FFF8F0;
  --focus-muted: #D8CDC3;
  --focus-accent: #B5EAD7;
  --focus-warning: #FFBE8E;

  --r-xs: 6px;
  --r-sm: 10px;
  --r-md: 14px;
  --r-lg: 20px;
  --r-xl: 28px;
  --r-full: 999px;

  --shadow-soft: 0 2px 10px rgba(79, 70, 63, 0.06);
  --shadow-card: 0 6px 24px rgba(143, 93, 183, 0.12);
  --shadow-float: 0 12px 36px rgba(79, 70, 63, 0.16);
  --shadow-button: 0 3px 12px rgba(143, 93, 183, 0.28);

  --focus-ring: 0 0 0 3px rgba(143, 93, 183, 0.36);
}
```

## 12 · 落地检查清单

### 品牌

- [ ] 3D Logo、Flat Logo、Mono Logo 三套版本齐全。
- [ ] App 图标在 32px、48px、128px 下仍可识别。
- [ ] Logo 有浅底、深底、单色场景测试。
- [ ] 早期传播页面使用 `嘟豆豆 Dodoudou` wordmark。

### 色彩

- [ ] 主按钮不再使用浅紫承载白字。
- [ ] 正文和关键辅助文字达到 WCAG AA 对比度。
- [ ] 成功、警告、错误、信息都有深色文字 token。
- [ ] 状态表达包含图标或文字，不只靠颜色。

### 编辑器

- [ ] 画布区域是编辑器视觉第一优先级。
- [ ] 当前格子、当前行、当前工具状态足够清晰。
- [ ] 色板显示色号、数量、选中态和完成态。
- [ ] Bottom Sheet 不遮挡关键画布操作。
- [ ] 工具栏按钮触控区域不小于 44x44px。

### 专注模式

- [ ] 支持深色专注界面。
- [ ] 当前颜色、行列、剩余数量足够醒目。
- [ ] 支持大字模式和左手模式。
- [ ] 支持防误触。

### 动效

- [ ] 图纸生成动效总时长不超过 900ms。
- [ ] 大图案不使用逐格无限 stagger。
- [ ] 支持 `prefers-reduced-motion`。

### 内容

- [ ] 空状态有明确行动按钮。
- [ ] 不依赖 emoji 作为主要插画系统。
- [ ] 参数说明讲清对珠子数量、难度、时间的影响。
- [ ] 下载页提供图纸、材料清单、色号表和分享图。

## 总结

v2 的核心不是把 v1 变得更可爱，而是让它变得更能用。

最终视觉方向：

```text
首页像温柔手工坊，
编辑器像清晰工作台，
专注模式像陪你完成作品的制作面板。
```

这套方向能保留 Dodoudou 的亲和力，同时避免掉进“浅色可爱但不专业”的问题。
