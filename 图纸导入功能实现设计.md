# 图纸导入功能实现设计

更新时间：2026-06-08

## 1. 目标

图纸导入功能的核心目标不是“把外部图片存进项目”，而是把外部拼豆图纸还原成当前项目已经使用的 `PatternResult`：

```ts
type PatternResult = {
  width: number;
  height: number;
  cells: PatternCell[];
  palette: PatternPaletteEntry[];
  stats: {
    totalCells: number;
    colorCount: number;
  };
};
```

这样导入后的图纸可以直接复用现有能力：

- 图纸预览
- 手动编辑
- PNG 下载
- 物料清单
- 库存检查
- 替代色
- 专注模式
- 画册发布

导入模块应该把“外部识别复杂度”隔离在 `pattern-import` 目录中，最终只向工坊交付标准 `PatternResult` 和一份可选的导入诊断信息。

## 2. 市面拼豆图纸的常见构成元素

常见拼豆图纸大致由以下元素组成：

| 元素 | 常见形态 | 对导入的意义 |
|---|---|---|
| 图案网格 | 正方形网格、带 5/10 格粗分割线、带坐标尺、无坐标纯像素图 | 决定 `width`、`height`、每格位置和颜色 |
| 格内信息 | 纯色块、色块 + 色号、色块 + 符号、仅符号、仅数字 | 决定每个 cell 归属哪个颜色 |
| 图例 / 色卡 | 颜色块 + 色名 / 色号 / 数量，或符号 + 色号 + 数量 | 是识别色号系统的最高可信来源 |
| 物料清单 | `颜色 - 数量`、`Bead Color/# Needed`、品牌色号列表 | 用来校验还原后的颜色统计 |
| 页眉信息 | 标题、作者、品牌、水印、工具名、pegboard 类型 | 用来推断来源和品牌 |
| 棋盘 / 模板信息 | 大方板、圆板、六边形板、多个拼板分区 | 决定有效区域和透明外部格 |
| 多页分割 | 一张大图拆成多页、分象限、分页坐标 | 需要拼接或让用户选择页块 |
| 非图纸说明 | 熨烫说明、材料购买说明、版权标识 | 识别时要排除 |

外部样例上能看到稳定趋势：

- Perler 官方项目页和 PDF 会给出图案、pegboard 类型、颜色名和每种颜色数量。
- Hama 的 Inspiration 页面以可下载图案为主，存在官方图案和用户图案两类来源。
- 生成器类网站通常强调 PDF 导出、网格图、图例和颜色数量。
- 第三方色表会把 Perler、Hama、Artkal、MARD 等品牌的颜色、HEX 和跨品牌对应放在一起。

参考来源：

- Perler 项目页与项目 PDF：`https://perler.com/blogs/projects/color-wheel-and-shapes`
- Perler 2025 颜色参考 PDF：`https://perler.com/content/uploaded_images/Perler_Bead-Color-Reference_2025.pdf`
- Hama Inspiration：`https://www.hama.dk/inspiration/`
- Bitbead 跨品牌色表：`https://bitbead.pomodiary.com/en/colors`

## 3. 输入格式优先级

不要一开始做“万能图纸识别”。建议分层支持：

### P0：结构化导入

优先支持可 100% 还原的格式：

- Dodoudou 自有 JSON
- Dodoudou 导出的 PNG 隐藏元数据
- CSV / TSV 矩阵
- 调色板 CSV

价值：

- 快速形成完整导入闭环。
- 为后续图像识别提供标准测试样例。
- 以后 Dodoudou 导出的图纸可以被无损再导入。

### P1：清晰截图 / PNG / JPG

支持最常见的用户场景：

- 用户从小红书、Pinterest、网页保存的图纸图片。
- 图中有完整规则网格。
- 图例和网格在同一张图中。
- 图例清晰，颜色块和色号可见。

### P2：PDF

支持：

- PDF 文本层解析。
- PDF 页渲染成图片后走图像识别。
- 多页图纸选择和拼接。

### P3：拍照 / 扫描图纸

支持：

- 透视矫正。
- 旋转修正。
- 阴影和纸张颜色校正。
- 手动框选网格与图例。

## 4. 推荐目录结构

```text
src/lib/pattern-import/
  types.ts                 # 导入中间模型
  validate.ts              # PatternResult 校验和统计重建
  structured.ts            # JSON / CSV / Dodoudou PNG 元数据导入
  image-decode.ts          # 图片 / PDF 页转 Canvas ImageData
  layout-detect.ts         # 页面区域识别：网格、图例、说明文字
  grid-detect.ts           # 网格识别、矫正、行列推断
  legend-detect.ts         # 图例识别：色块、符号、色号、数量
  cell-read.ts             # 单元格采样、格内文字/符号读取
  color-system-detect.ts   # 品牌与色号系统推断
  color-resolve.ts         # 外部色号 -> 内部 hex / brand code
  reconstruct.ts           # 生成 PatternResult
  confidence.ts            # 单元格、图例、色号系统置信度
  diagnostics.ts           # 导入报告、问题列表
```

页面层建议新增：

```text
src/pages/workshop/import/
  WorkshopImportPage.tsx
  WorkshopImportPage.module.css
```

路由建议：

```text
/workshop/import
/workshop/import/:projectId
```

首页当前已有“导入图纸”入口，占位逻辑在：

- `src/pages/workshop/WorkshopHomePage.tsx`
- `src/pages/workshop/components/WorkshopHomeToolbar.tsx`

## 5. 导入中间模型

`PatternResult` 不适合直接承载识别过程。导入阶段需要保留证据、来源和冲突信息。

建议新增中间模型：

```ts
export type PatternImportSourceType = 'dodoudou-json' | 'csv' | 'image' | 'pdf';

export type PatternImportDocument = {
  sourceType: PatternImportSourceType;
  fileName: string;
  pages: PatternImportPage[];
};

export type PatternImportPage = {
  pageIndex: number;
  width: number;
  height: number;
  imageData?: ImageData;
  textItems?: PatternImportTextItem[];
};

export type PatternImportGridCandidate = {
  pageIndex: number;
  bounds: { x: number; y: number; width: number; height: number };
  transform?: DOMMatrix;
  columns: number;
  rows: number;
  cellWidth: number;
  cellHeight: number;
  score: number;
};

export type ImportedPaletteEntry = {
  importColorKey: string;
  symbol?: string;
  rawCode?: string;
  rawName?: string;
  rawCount?: number;
  swatchHex?: string;
  resolvedBrand?: ColorSystem;
  resolvedCode?: string;
  resolvedHex?: string;
  resolution: 'exact' | 'alias' | 'nearest' | 'external' | 'unresolved';
  confidence: number;
  issues: PatternImportIssue[];
};

export type ImportedCell = {
  x: number;
  y: number;
  sampledHex?: string;
  symbolText?: string;
  rawCodeText?: string;
  importColorKey?: string;
  resolvedHex?: string;
  resolvedCode?: string;
  isExternal?: boolean;
  confidence: number;
  evidence: PatternImportCellEvidence[];
};

export type PatternImportAnalysis = {
  document: PatternImportDocument;
  selectedGrid: PatternImportGridCandidate | null;
  detectedBrand: ColorSystem | 'external' | null;
  brandConfidence: number;
  palette: ImportedPaletteEntry[];
  cells: ImportedCell[];
  issues: PatternImportIssue[];
};

export type PatternImportResult = {
  patternResult: PatternResult;
  analysis: PatternImportAnalysis;
};
```

最终项目保存时：

- `PatternResult` 进入现有 `WorkshopProjectRecord.patternResult`。
- 导入诊断信息可以先只存在内存中，不进入持久化。
- 如果要持久化，建议新增 `WorkshopProjectRecord.importMeta?`，不要污染 `PatternResult`。

## 6. 识别管线

### 6.1 文件解码

输入文件先统一转为页面模型：

```text
File
  -> PatternImportDocument
  -> pages[]
  -> imageData / textItems
```

处理原则：

- 图片：读取 EXIF 方向，绘制到 Canvas，限制最大边长，保留原始尺寸。
- PDF：优先抽文本层，再把每页渲染为图片。
- CSV：直接解析矩阵和色表。
- Dodoudou JSON / PNG 元数据：直接生成 `PatternResult`。

### 6.2 页面区域识别

识别目标：

- 主图纸网格区域。
- 图例区域。
- 物料清单区域。
- 说明文字区域。

网格识别特征：

- 大量等距横线/竖线。
- 近似正方形 cell。
- 5/10 格粗线或坐标尺。
- 大面积重复色块。

图例识别特征：

- 小色块重复排列。
- 色块旁边有短文本。
- 文本中出现数字、字母色号、颜色名、数量。
- 常见关键词：`Bead Color`、`Needed`、`颜色`、`色号`、`数量`、`PCS`、`颗`。

### 6.3 网格还原

网格还原输出必须确定：

- `columns`
- `rows`
- 每格中心点坐标
- 每格有效范围
- 外部空白 / 透明区域

算法建议：

1. 图像灰度化和边缘增强。
2. 用形态学提取横线、竖线。
3. 通过线间距聚类得到候选网格。
4. 透视矫正后重新检测一次。
5. 使用坐标尺或粗分割线校准行列数。
6. 如果检测不稳定，让用户手动拖四角和输入行列数。

必须支持非正方形图纸。当前 `PatternResult` 已支持 `width` 和 `height` 不同，不要把导入限制为 `canvasSize x canvasSize`。

### 6.4 单元格读取

每个 cell 读取多种证据：

| 证据 | 说明 | 可信度 |
|---|---|---|
| 图例符号匹配 | 格内符号对应图例符号 | 高 |
| 格内色号 OCR | 格内直接写品牌色号 | 高，但小字容易错 |
| 色块采样 | 从 cell 中心安全区域取颜色 | 中 |
| 图例色块匹配 | cell 色块匹配图例 swatch | 中高 |
| 邻近一致性 | 相邻区域颜色一致 | 辅助 |
| 物料清单数量 | 重建统计和清单数量一致 | 全局校验 |

采样建议：

- 避开网格线，只取中心 50%-70% 区域。
- 如果格内有文字，先用中值 / 聚类排除文字像素。
- 对照片类输入先做白平衡和亮度归一。
- 单格输出 `sampledHex` 和 `confidence`，不要直接假设采样色就是最终豆子色。

### 6.5 图例识别

图例是色号系统识别的核心。每条图例需要尽量拆成：

```text
symbol / swatch / rawCode / rawName / rawCount
```

示例：

```text
A  MARD A01  白色  128
●  Red - 428
G11  36颗
80-19001 WHITE 93
```

解析规则：

- `rawCount` 通常是最后一个整数，旁边可能有 `pcs`、`颗`、`Needed`。
- `rawCode` 通常是短字母数字组合，例如 `A01`、`G11`、`S4`、`80-19001`。
- `rawName` 是去掉数量和色号后的文本。
- 如果只有颜色名和数量，也要保留，后续可通过品牌色名别名表匹配。
- 如果只有符号和 swatch，也能通过 swatch 映射到内部品牌色。

## 7. 色号系统识别设计

当前项目的颜色系统基础是：

- 品牌 key：`MARD`、`COCO`、`MANMAN`、`PANPAN`、`MIXIAOWO`
- 运行时映射：`src/lib/pattern/colorSystemMapping.json`
- 查询能力：`getBrandPalette`、`getColorByBrandCode`、`getVendorCode`
- 统一颜色主键：当前使用 hex 作为 `colorId`

导入功能应该基于“外部色号归一化”扩展，而不是改掉现有色号系统。

### 7.1 色号归一化

新增规范化函数：

```ts
type NormalizedColorCode = {
  raw: string;
  normalized: string;
  compact: string;
  tokens: string[];
};
```

规则：

- 全角字符转半角。
- 去掉多余空格、冒号、中文冒号。
- 大小写统一。
- 中文数字和常见单位归一。
- `A-01`、`A 01`、`A01` 归一到同一 compact 值。
- `80 19001`、`80-19001` 归一到同一 compact 值，但保留显示格式。

### 7.2 品牌推断

对每个品牌计算分数：

```text
brandScore =
  exactCodeCoverage * 0.45
  + codeGrammarScore * 0.20
  + swatchColorScore * 0.20
  + brandTextHintScore * 0.10
  + countConsistencyScore * 0.05
```

分数来源：

- `exactCodeCoverage`：图例色号能在该品牌中精确命中的比例。
- `codeGrammarScore`：色号格式是否符合品牌习惯。
- `swatchColorScore`：图例色块和品牌色卡的 Lab 色差是否合理。
- `brandTextHintScore`：页面是否出现品牌名。
- `countConsistencyScore`：按该品牌还原后，颜色统计是否和物料清单接近。

决策：

- `score >= 0.85`：自动识别品牌。
- `0.60 <= score < 0.85`：给出推荐品牌，用户确认。
- `< 0.60`：进入“未知 / 外部色卡”模式。

### 7.3 色号解析优先级

单个图例色的解析顺序：

1. 用户手动指定品牌 + 色号精确命中。
2. 自动识别品牌 + 色号精确命中。
3. 外部别名表命中：色名、旧码、常见别称。
4. 图例 swatch 与该品牌色卡最近色匹配，且 DeltaE 在阈值内。
5. 跨品牌同 hex 映射。
6. 近似色推荐，必须用户确认。
7. 作为 `external` 颜色导入，保留 swatch hex 和原始色号。

### 7.4 重复码和冲突

项目里已经允许少量重复码，例如某些品牌色号可能暂时对应多个 hex。导入时不能静默选错。

处理方式：

- 如果一个 `brand + code` 对应多个 hex：
  - 有 swatch 时，用 swatch 色差排序。
  - 有颜色名时，用名称别名辅助。
  - 有数量时，用重建统计辅助。
  - 仍无法区分时标记为 `ambiguous`，进入用户确认。
- 如果 OCR 读出的色号不存在：
  - 给出编辑建议，例如 `S4` / `54` / `G11`。
  - 允许用户从品牌色卡中选择。
- 如果图例数量和重建数量不一致：
  - 标记该色为 `countMismatch`。
  - 在确认页显示差异。

### 7.5 外部色号模式

有些图纸来自 Perler、Hama、Artkal 或其他工具，不一定在当前五个品牌里。建议支持外部色号模式：

```ts
type ExternalPaletteColor = {
  sourceBrand?: string;
  rawCode?: string;
  rawName?: string;
  hex: string;
  nearestInternal?: {
    brand: ColorSystem;
    code: string;
    hex: string;
    deltaE: number;
  };
};
```

导入后有两种路径：

- 作为外部色卡保存，图纸可编辑和下载，但库存检查只显示未知。
- 用户选择转换到某个内部品牌色卡，系统逐色推荐最近色，用户确认后写入内部 `vendorCode`。

## 8. 图纸还原策略

还原时不要只依赖颜色采样。推荐做证据融合：

```text
cellColor =
  if symbol matches legend with high confidence:
    legendColor
  else if cell OCR code matches known code:
    codeColor
  else if sampled color matches legend swatch:
    legendSwatchColor
  else:
    nearest palette color
```

单格置信度：

```text
cellConfidence =
  symbolConfidence * 0.35
  + codeOcrConfidence * 0.25
  + colorDistanceConfidence * 0.25
  + neighborConsistency * 0.10
  + countConsistencyContribution * 0.05
```

生成 `PatternResult` 时：

- `x`、`y` 按网格坐标写入。
- 空白格写 `transparent`，并设置 `isExternal: true`。
- 已解析内部品牌的颜色写入：
  - `colorId`: hex
  - `hex`: hex
  - `vendorCode`: 当前品牌色号
- 未解析但允许外部导入的颜色：
  - `colorId`: hex 或 `external:<hash>`
  - `hex`: swatch hex
  - `vendorCode`: 原始 code 或空
  - `isExternal` 不应滥用；`isExternal` 当前语义更接近透明/外部空白。外部色卡建议用 import metadata 标记。
- `palette` 必须从 cells 重建，不能直接信任图例数量。
- 图例数量只作为校验和提示。

## 9. 用户确认流程

建议首页“导入图纸”进入四步流程。

### 9.1 选择文件

支持：

- `.json`
- `.csv`
- `.png`
- `.jpg`
- `.jpeg`
- `.webp`
- `.pdf`

### 9.2 识别图纸区域

展示原图，自动框出：

- 网格区域
- 图例区域
- 物料清单区域

用户可以：

- 切换候选网格。
- 手动拖四角。
- 输入行列数。
- 多页 PDF 选择页面或拼接顺序。

### 9.3 确认色号系统

展示：

- 自动识别品牌。
- 识别到的图例颜色。
- 未识别 / 冲突色号。
- 每个颜色的原始色号、推荐内部色号、数量差异。

用户可以：

- 切换品牌。
- 编辑 OCR 色号。
- 为未识别颜色选择品牌色。
- 保留外部色。

### 9.4 预览还原结果

展示：

- 原图和还原图并排。
- 低置信度格子高亮。
- 图例数量 vs 重建数量差异。
- “进入编辑器”或“保存为项目”。

保存后：

- `sourceType` 建议扩展为 `'import'`。
- `kind` 为 `'pattern'`。
- `status` 为 `'ready'`。
- `viewMode` 为 `'pattern'`。
- 跳转 `/workshop/result/:projectId`。

## 10. 与现有项目的接入点

需要调整的现有类型：

```ts
export type WorkshopProjectSourceType = 'blank' | 'upload' | 'gallery' | 'import';
```

建议新增项目字段：

```ts
export type WorkshopProjectRecord = {
  // existing fields...
  importMeta?: PatternImportProjectMeta | null;
};

export type PatternImportProjectMeta = {
  sourceType: PatternImportSourceType;
  fileName: string;
  detectedBrand: ColorSystem | 'external' | null;
  brandConfidence: number;
  unresolvedColorCount: number;
  lowConfidenceCellCount: number;
  importedAt: string;
};
```

注意：

- `PatternResult` 仍保持干净。
- 导入诊断详情可以不长期保存，避免 IndexedDB 和云端 payload 膨胀。
- 如果后续需要用户继续修复导入问题，再考虑把完整 `analysis` 存到单独 store。

## 11. 校验规则

导入完成前必须跑校验：

```ts
type PatternImportValidationReport = {
  ok: boolean;
  width: number;
  height: number;
  totalCells: number;
  colorCount: number;
  emptyCellCount: number;
  unresolvedColorCount: number;
  lowConfidenceCellCount: number;
  countMismatches: Array<{
    importColorKey: string;
    legendCount: number;
    reconstructedCount: number;
  }>;
  issues: PatternImportIssue[];
};
```

阻断错误：

- 没有识别到网格。
- 行列数无法确定。
- 有颜色无法解析且用户没有选择“作为外部色导入”。
- 生成的 `PatternResult.cells` 坐标越界。

非阻断 warning：

- 图例数量和还原数量不一致。
- 品牌识别置信度低。
- 存在低置信度格子。
- 图片分辨率过低。
- 图纸可能被透视或裁切。

## 12. 迭代计划

### M0：无损结构化导入

目标：

- 新增 `pattern-import` 基础类型。
- 支持 Dodoudou JSON 导入。
- 支持 CSV 矩阵导入。
- 能保存为项目并进入结果页。
- 添加 `PatternResult` validator。

验收：

- 导入后编辑器、下载、物料清单、专注模式可用。
- `npm run check` 通过。

### M1：自有导出可回导

目标：

- 在 Dodoudou 导出的 PNG 中写入可选元数据或旁路 JSON。
- 导入自有 PNG 时优先读取元数据。
- 无损还原 `PatternResult`。

价值：

- 用户分享 Dodoudou 图纸后可以重新导入。
- 为外部识别做对照组。

### M2：清晰图片的网格 + 图例识别

目标：

- 支持截图 / PNG / JPG。
- 自动识别矩形网格。
- 支持纯色块图纸。
- 支持图例 swatch + 数量。
- 用户可手动输入行列数修正。

验收：

- 对清晰图纸，网格行列识别准确率达到 95% 以上。
- 色块分类正确率达到 90% 以上。

### M3：OCR 色号和符号图例

目标：

- 支持图例文字 OCR。
- 支持格内色号 / 符号识别。
- 支持符号图例映射。
- 支持品牌自动推断。

验收：

- 常见清晰 PDF / 截图中，图例色号识别准确率达到 90% 以上。
- 品牌推荐可解释，低置信度必须进入确认。

### M4：PDF 多页和拼接

目标：

- PDF 页面渲染。
- PDF 文本层辅助识别图例和数量。
- 多页网格选择与拼接。

验收：

- 用户可以从多页 PDF 选择图纸页。
- 图例在第一页、网格在后续页时仍可还原。

### M5：拍照图纸和高级修复

目标：

- 透视矫正。
- 阴影校正。
- 手动四角框选。
- 低置信度格子局部重识别。

验收：

- 手机拍摄纸质图纸可通过人工辅助完成导入。

## 13. 成功指标

算法指标：

- 清晰截图网格识别成功率 >= 95%。
- 清晰截图 cell 颜色还原准确率 >= 90%。
- 有图例时 palette 数量误差 <= 2%。
- 有品牌色号时品牌识别 Top1 准确率 >= 90%。
- 低置信度格子必须可定位、可修正。

产品指标：

- 导入后能进入结果页。
- 导入后能进入编辑器。
- 导入后能下载图纸。
- 导入后能生成物料清单。
- 导入后库存检查不崩溃。
- 无法自动识别时，用户能通过确认页完成导入，而不是直接失败。

性能指标：

- 结构化导入小于 1 秒。
- 100x100 清晰图片导入分析尽量控制在 3 秒内。
- PDF 和 OCR 可以异步处理，并显示进度。

## 14. 风险与应对

| 风险 | 影响 | 应对 |
|---|---|---|
| 外部图纸格式差异极大 | 自动识别无法全覆盖 | 分阶段支持，必须保留手动修正 |
| OCR 把色号读错 | 色号系统错误，整图偏色 | OCR 结果必须经过品牌色卡校验和用户确认 |
| 颜色采样受压缩/阴影影响 | 色块分类错误 | 优先使用图例和符号，采样只作为证据之一 |
| 重复色号或别名 | 同码多色导致误选 | swatch / 名称 / 数量辅助，无法区分时阻断确认 |
| 外部品牌不在内部色卡 | 库存和替代色无法直接使用 | 支持外部色导入，再提供转换到内部品牌 |
| 多页 PDF 拼接复杂 | 大图纸还原错位 | 先支持单页，后支持用户手动排序拼接 |
| 把识别诊断写入 PatternResult | 下游被复杂字段污染 | PatternResult 保持不变，诊断单独存储 |

## 15. 推荐实施顺序

1. 新增 `pattern-import` 类型、validator 和 `reconstructPatternResult`。
2. 打通首页“导入图纸”入口和 `/workshop/import` 页面。
3. 支持 Dodoudou JSON / CSV 导入，先完成项目保存闭环。
4. 在下载图纸时预留自有元数据方案，实现自有图纸回导。
5. 做清晰图片网格识别和手动行列修正。
6. 做图例 swatch 识别和颜色分类。
7. 做 OCR 色号、品牌推断和冲突确认。
8. 做 PDF 页面渲染和文本层解析。
9. 做拍照矫正、多页拼接和高级修复。

这条路线能先拿到稳定闭环，再逐步提高外部图纸识别能力；每一步都能独立验证，不需要一次性重写现有工坊流程。
